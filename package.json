{
  "name": "diabolocom-json-viewer",
  "version": "1.0.0",
  "description": "A small test...",
  "main": "index.js",
  "scripts": {
    "clean": "shx rm -rf dist/",
    "copy": "shx mkdir dist/ && shx cp src/*.* dist/",
    "browserify": "browserify src/js/app.js -d -p [ tsify ] -o dist/bundle.js",
    "build": "npm run clean -s && npm run copy -s && npm run browserify -s",
    "start": "http-server dist/ -o -c0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avine/diabolocom-json-viewer.git"
  },
  "author": "Stéphane Francel <contact@avine.io>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/avine/diabolocom-json-viewer/issues"
  },
  "homepage": "https://github.com/avine/diabolocom-json-viewer#readme",
  "devDependencies": {
    "browserify": "^14.3.0",
    "http-server": "^0.10.0",
    "shx": "^0.2.2",
    "tsify": "^3.0.1",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "jquery": "^3.2.1"
  }
}
